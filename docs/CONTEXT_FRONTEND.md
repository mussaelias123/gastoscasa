# Contexto: Frontend (HTML / CSS / JS)

> Leer junto con `CLAUDE.md`. Para cambios visuales y de interactividad.

## Archivos del dominio
- `static/style.css` (~5890 líneas): estilos globales + variables.
- `static/app.js` (~1220 líneas): interactividad cliente, AJAX, edición inline.
- `static/calendario.js` (826 líneas): módulo Calendario. Solo se carga en `/calendario` (vía `{% block scripts %}` de `base.html`, con el mismo `?v={{ static_version }}`).
- `static/lactancia.js` (746 líneas): módulo Lactancia. Solo se carga en `/lactancia` (mismo mecanismo).
- `static/rutina.js` (~760 líneas): módulo Rutina. Solo se carga en `/rutina` (mismo mecanismo). Incluye las DEFINICIONES de rutina por etapa (`ETAPAS`, constantes JS).
- `static/rutina-actividades.js` (502 líneas): datos puros — `window.RUTINA_ACTIVIDADES = {m2, m3, m46}`, 82 actividades de estimulación de la guía "Estimulación Temprana" (Karina Rivera). Se carga antes de `rutina.js`.
- `static/home.js` (~330 líneas): página Inicio (`/`). Solo se carga ahí (mismo mecanismo). Agrega `body.home-body` y maneja los sheets genéricos (`[data-sheet]` abre `#home-sheet-<nombre>` + `#home-overlay` con `.is-abierta`; cierra ✕/overlay/ESC); expone `window.cerrarHomeSheet(nombre)` (la llama `initFormAjax` de app.js tras un alta inline). Tarjeta Lactancia (`initLactancia()`): estado local `window.LAC_HOME` `{heladera, freezer_primera}`; flatpickr del form (fecha `maxDate:'today'`, hora 24h `disableMobile:true` — copias mínimas de lactancia.js, que en `/` NO se carga); submit AJAX a `/api/lactancia/crear` y `[data-lac-usar]` → POST `/api/lactancia/<id>/cerrar` (motivo=usada); la respuesta es `_lac_payload()` completo → se proyecta a LAC_HOME y `renderLacHome()` reconstruye `#home-lac-listas` con `createElement`/`textContent` (espeja el markup Jinja de index.html); toast propio (`lacToast`, reusa `.toast`+`.lac-toast-*`) y `Notif.refrescar()` tras cada mutación.
- `templates/`:
  - `base.html` — layout. Solo el header va en `.site-topbar` (sticky, `top:24px` si hay dev-banner): `☰` menú (izq) · marca centrada · `.header-acciones` (tema · Salir · avatar `.header-user-photo` con `title` · campana `🔔`) a la derecha. Ya no hay `.site-nav`: la navegación vive en la SIDEBAR desplegable (`#sidebar`, overlay izquierdo, mobile Y desktop) y las notificaciones en el PANEL derecho (`#notif-panel` + `#notif-lista`). Ambos comparten `#drawer-backdrop` (exclusión mutua) y los maneja `initDrawers()`; la campana usa `window.Notif` (ver `CONTEXT_NOTIFICATIONS.md`). Todas extienden esto.
  - `index.html` — página Inicio, ruta `/` (home de la app). Grid de 4 tarjetas `.home-card`: Gastos funcional (saldos mini + form de movimiento), Lactancia funcional (listas Heladera completa + PRIMERA del freezer FIFO server-rendered desde `lac_home`, cada ítem con "✓ Usada" `[data-lac-usar]`; vencimiento relativo — heladera en horas, freezer en días — y "Extraída HH:MM · fecha"; form de extracción vía partial `_form_lactancia.html`; home.js re-renderiza espejando este markup) y Rutina/Calendario placeholder (llegan en etapas siguientes). SIN scroll de página en desktop NI mobile (a diferencia de los módulos, que solo lo fijan ≥900px); una tarjeta puede scrollear adentro (`.home-card-body`). Desktop ≥900px: 3 col × 2 filas (col A Gastos/Rutina, col B Lactancia row-span 2, col C Calendario row-span 2), forms inline. Mobile <900px: 2 col (Gastos y Rutina span 2, Lactancia|Calendario), cada form vive en un bottom-sheet (`.home-sheet`, lo abre su botón `＋ .home-btn-nuevo`). Cada form existe UNA sola vez en el DOM. Inyecta `window.GASTOS_FIJOS` + `window.LAC_HOME` y carga `home.js`.
  - `_form_movimiento.html` — partial: el form rápido de movimiento (única definición; ids únicos). Lo incluyen gastos.html (tal cual) e index.html (con `{% with form_modo='inline' %}` → `data-modo="inline"`, y sin `mes`: el hidden es condicional `{% if mes is defined %}`).
  - `_tarjeta_saldos_mini.html` — partial: SOLO la tabla de saldos (6 celdas `saldo-*` que actualiza `actualizarSaldos()`). Lo incluyen gastos.html (dentro de `.tarjeta-saldos`, que conserva date picker y gauges propios) e index.html.
  - `_form_lactancia.html` — partial: el form de alta de extracción del módulo Lactancia (única definición; ids `lac-ex-*`). Lo incluyen lactancia.html (panel Cargar, lo inicializa lactancia.js) e index.html (sheet de la tarjeta Lactancia, lo inicializa home.js). POST real a `/api/lactancia/crear` (funciona sin JS); el backend EXIGE fecha y hora de extracción no vacías.
  - `gastos.html` — pantalla del módulo Gastos, ruta `/gastos` (saldos + form rápido + tags de gastos fijos + tabla). El form y la tabla de saldos vienen de los partials `_form_movimiento.html` y `_tarjeta_saldos_mini.html`. Los fijos del mes se muestran como pills con wrap (`.fijos-tags`): impagos primero (clickeables: precargan el form rápido vía `initTagsFijos()`), pagados al fondo grisados con ✓ + monto + badge persona; último pill = link "⚙ Administrar" (no hay header propio de sección). Desktop ≥900px: página sin scroll (mismo patrón que Calendario, `body:has(.layout-desktop)` alto 100dvh), la tabla de movimientos scrollea sola dentro del card con thead sticky, y la col izquierda se compacta para entrar en el viewport. Gauges: leyenda a la derecha de cada gauge (`.gauge-info`), no debajo.
  - `editar.html` — edición completa de movimiento.
  - `resumen.html` — dashboard mensual (Chart.js), 5 secciones: saldos, sueldos (Elías vs Mari, evolución 6 meses), análisis de gastos, envíos, fijos.
  - `settings.html` — ajustes. Layout 2 columnas (`.settings-layout`): izquierda scrollea (General, Paleta, Backups, Gastos fijos); derecha `.settings-aside` sticky con el Monitor de recursos (relojitos). Responsive: apila en 1 columna < 900px. **No incluye** ngrok/OAuth/estado-entorno (se manejan en `config.json`, fuera de la UI).
  - `calendario.html` — módulo Calendario (tareas del hogar): agenda Pendientes + calendario mensual + alta rápida + modales Completar / Editor / Todas / Confirmar. Inyecta `window.CAL_DATOS/CAL_AREAS/CAL_RESPONSABLES` (tojson) y carga `calendario.js`. El form de alta rápida es un `<form>` real (POST `/api/actividades/crear`, funciona sin JS, pero pierde el date picker: el input queda de texto libre validado ISO por el backend). Desktop ≥900px: `body.cal-body` (la agrega el JS) fija alto = viewport, sin scroll de página. Fechas: los 3 date inputs (`cal-qa-ultima`, `cal-comp-fecha`, `cal-ed-ultima`/`cal-ed-limite`) son `type="text"` + flatpickr (mismo patrón que `inicializarFechaHoy()` de `app.js`), no `<input type="date">` nativo: valor real ISO `Y-m-d` vía `altInput`, display `d/m/Y`. `cal-comp-fecha` tiene `maxDate: 'today'`.
  - `lactancia.html` — módulo Lactancia (banco de leche de León). **Camino estándar de la leche**: toda extracción entra por HELADERA (un solo form real de alta, con fecha/hora de extracción); al freezer se pasa SIEMPRE la combinación de partidas de heladera tildadas (checkbox por fila, tildado por defecto si reciente + botón ⬆ en el header del panel → POST `/api/lactancia/freezar`). Sin alta directa a freezer ni traspaso individual. Estructura: aviso al entrar + panel Cargar (form del partial `_form_lactancia.html`, compartido con el Inicio) + Resumen + listas FIFO Freezer/Heladera + historial `<details open>` + modales Cerrar-con-fecha / Más-opciones / Editor / Confirmar. Inyecta `window.LAC_DATOS` (tojson) y carga `lactancia.js`. Cierres one-click con fecha=hoy + toast con botón Deshacer (8 s, llama a `/reabrir`; en una freezada revierte la combinación completa). TODAS las tarjetas (freezer, heladera e historial) muestran "Extraída HH:MM · fecha" — hora primero, fecha después (pedido de Mari 2026-07-11, que REEMPLAZA su pedido anterior de ocultar la hora en heladera; helper `extraidaTxt()`); vencimiento relativo ("Vence en 5 h"). Sin disclaimer médico (lo pidió Elías 2026-07-06). Desktop ≥900px: `body.lac-body`, 3 columnas (cargar+resumen | freezer arriba/heladera abajo | historial), 100dvh sin scroll de página. Fechas: flatpickr `maxDate: 'today'`. Horas (`lac-ex-hora`, `lac-ed-hora`): flatpickr `enableTime+noCalendar+time_24hr` (`H:i`) **+ `disableMobile: true`** — sin eso, flatpickr en celulares se auto-reemplaza por el `<input type="time">` NATIVO (12h AM/PM en iOS, desborda la tarjeta `+Cargar`). Los inputs nativos que flatpickr sí crea en móvil (fechas, clase `.flatpickr-mobile`) tienen CSS defensivo: estilo estándar + `max-width:100%` + `appearance:none`.
  - `rutina.html` — módulo Rutina (rutina diaria de León + agendas de mamá/papá, horarios en cascada). Columna única mobile-first (max 480px centrada). 7 zonas: topbar sticky propio (header ⏰+fecha+edad / chips multi-select de usuarios) → selector de día L-D → selector de etapa (2m/3m/4m-guardería) → aviso contextual (si no es "hoy real") → tarjetas "Ahora" (solo hoy+etapa actual, con barra de progreso, botón "⏱ Empezó ahora" en la de León, y emoji clickeable = scrollea la línea de tiempo hasta ese ítem con destello `.is-foco`) → LIENZO DE COLUMNAS (una línea de tiempo por persona activa, estilo agenda: eje de tiempo real 1.6 px/min, gutter de horas + gridlines, ítems absolutos con alto = duración que quedan LADO A LADO cuando son simultáneos (línea principal "hora emoji título · dur" con título 0.95rem; en bloques ≥68 px vuelve el `sub` 0.85rem clampeado a las líneas que entran; ancho de columna con tope `COL_MAX` 380px — en pantallas anchas el lienzo se ciñe a la información vía `max-width` inline en `.rut-canvas`/`.rut-canvas-head`), línea "ahora" cruzando todo; tap en ítem = popover superpuesto con detalle `sub` + editor −15/+15/Ahora si es editable o nota informativa si es link — SIN botón OK: tocar cualquier parte del popover lo cierra (`data-cerrar`/`data-q-cancelar` en el contenedor; los botones y los inputs de hora se chequean antes en el handler). La hora del editor son DOS inputs numéricos (`.rut-hora-in`, `data-hora-h/m`): ruedita del mouse ±1 h/±5 min (`wheel` delegado `{passive:false}`) y teclado numérico al foco (`change` delegado aplica con `ajustar()`; las nocturnas conservan su offset ≥1440); el tick de 30 s se saltea también con `editando` abierto. SOLAPES dentro de una columna (el día real se corrió): carriles estilo agenda — cluster de ítems que se pisan reparte el ancho (`left/width` % inline), los `dur 0` no compiten. DRAG ESTILO TEAMS (ítems editables, fuera del modo edición): arrastrar el cuerpo (`data-drag`) MUEVE el inicio (→ `ajustar()`), la manija inferior (`.rut-item-grip`, `data-grip`, `touch-action:none`) ESTIRA la duración; los links puros (atados a León: tetas/upa/paseo) NO se arrastran y se distinguen con `.rut-item--fijado` (borde punteado) + 🔗 `.rut-item-candado` (→ `ajustarDur()` + tabla `rutina_dur`, POST `/api/rutina/duracion`; "↺ Plan original" también la limpia; `calcular()` aplica el override en cadena de León/clock/link propio/custom — las nocturnas quedan fijas en 25'). Mouse: drag directo con umbral 4 px (así no se come el tap del popover; `seArrastro` suprime el click fantasma); táctil: mover = TOQUE SOSTENIDO 350 ms (si el dedo se desplaza antes gana el scroll; `touchmove` no-pasivo con `preventDefault` solo en drag activo), estirar = directo desde la manija. Snap 5 min; durante el gesto solo se mueve el bloque agarrado (DOM directo) y la cascada se reacomoda al soltar; el tick de 30 s también se saltea con `drag` activo. Tarjetas "Ahora": fallback "durmiendo" — si no hay ítem en curso pero el último `dur 0` del usuario ya empezó (o `now < 300`), se muestra como tarjeta abierta (mamá/papá "A dormir" toda la noche, no solo León); botón "⏳ Aún en {anterior}" (`.rut-btn-aun`, `data-aun`/`data-aun-cur`) junto a "⏱ Empezó ahora": lo real va atrasado — estira la actividad ANTERIOR hasta ahora +10' (`ajustarDur`) y clava la actual a continuación (`ajustar`); solo si la actual está de verdad en ventana (dur > 0, no fallback nocturno) y la anterior es editable; "↺ Plan original" resetea la fecha+etapa; "✎ Editar" activa el modo edición: ✕ por ítem con popover "¿solo hoy o siempre?", "＋ Añadir tarea" con form arriba del lienzo y lista de quitadas con ↩ Restaurar). La franja "🌙 Madrugada" (tomas nocturnas como filas `.rut-fila` clásicas con editor inline) va ARRIBA del lienzo: el día nuevo arranca después de las 00:00 con esas tomas (a escala sumarían ~10 h de scroll vacío) → card de tips. TARJETA "HOY POR CALENDARIO" (`#rut-cal` en `.rut-selectores`, tras las etapas): si una actividad del Calendario vence hoy y se está mirando el día real, tarjeta monocromática (tono deco-3/muted, `.rut-cal-card`) que ofrece añadirla — click abre el form de añadir prefijado ahí mismo (`formAdd.en = 'cal'|'filas'` decide dónde vive; `clickTimeline` compartido entre `#rut-filas` y `#rut-cal`), ✕ la descarta por hoy (localStorage `rutina-cal-v1`, solo guarda la fecha actual); se oculta sola si ya hay una tarea añadida con el mismo título hoy. NO completa la actividad en Calendario. Los bloques despiertos se llaman por su actividad ("Juego 1", "Paseo", "Upa y calma") — se eliminó la palabra "Vigilia" (pedido de Mari). Es puro esqueleto: los contenedores `id="rut-*"` los llena `rutina.js`. Inyecta `window.RUT_DATOS` (ajustes + tareas añadidas + ocultos + rango) y carga `rutina-actividades.js` + `rutina.js`. Re-render cada 30 s (reloj vivo + sync entre teléfonos; el tick se saltea en modo edición para no pisar lo tipeado). Selección de UI (usuarios/día/etapa) en localStorage `rutina-ui-v1` (por dispositivo); ajustes/tareas/ocultos en SQLite (compartidos). Desktop ≥900px: `body.rut-body` (la agrega el JS; `:has()` cubre el primer paint), 100dvh sin scroll de página, grid de 2 columnas — izquierda `minmax(340px,420px)` = topbar (ya no sticky) + selectores + tarjetas "Ahora"; derecha = "El día completo" con scroll interno y `.rut-tl-head` sticky. En la rutina: mamá acompaña TODAS las tetas de León (links `mama-*`, "no hay tiempo libre durante la alimentación"), lleva el PASEO entre semana ("Paseo con León"; los findes es "Paseo familiar" de los tres — `mamaFinde` de 2m/3m se DERIVA de la lista de semana cambiando solo ese ítem, post-procesado tras el literal `ETAPAS`) y hace el BAÑO de León (link `bano`/`t-bano`/`g-bano`/`f-bano`, sigue con el ritual); las duchas de mamá y papá van a la NOCHE con León dormido (mamá: link a `noche`; papá: ítems clock `*-ducha` editables). **REGLA: toda actividad de un adulto que involucre a León debe ser un `link` a un ítem de la cadena de León, NUNCA un `clock`** — los ítems de León no se solapan entre sí (cascada), así que es imposible que dos adultos hagan cosas distintas con León a la vez; dos adultos linkeando el MISMO ítem (paseo familiar) = actividad compartida, válido. **Dos sabores de `link`**: (a) PURO, sin `id` propio (tetas, upa, paseo) → hereda horario de León y NO es editable, id derivado `mama-*`/`papa-*`; (b) CON `id` propio (`m-gym`, `m-ducha`, `pf-tareas`…) = actividad PROPIA del adulto que por defecto usa esa ventana de León pero ES editable: un ajuste la clava a horario fijo, "↺ Plan original" la vuelve a enganchar. Los `clock` (trabajo, cenas, duchas de papá) son siempre editables.
  - `login.html` — pantalla pre-OAuth.
  - `404.html`, `405.html` — errores.

## Paleta de colores (regla NO negociable)

**Config-driven desde Fase 1 de dark mode.** Los 23 colores base se guardan en `config.json` como `paleta_light` y `paleta_dark`. `base.html` los inyecta en un `<style>` en el `<head>` antes del stylesheet, así:

```css
:root { --color-acento: #4f46e5; ... }          /* desde cfg.paleta_light */
html[data-theme="dark"] { --color-acento: #6366f1; ... }  /* desde cfg.paleta_dark */
```

`static/style.css → :root` sigue definiendo los mismos colores como **fallback** (por si config.py no carga). Regla intacta: **NUNCA usar colores literales**. Siempre `var(--color-...)`.

**Filosofía de grises (regla de diseño):** Chrome estructural/decorativo (botones, nav, headers, bordes, fondos) usa solo `--color-deco-1..4`. Los colores llamativos (`acento`, `exito`, `peligro`, `persona-*`, `moneda-*`) se reservan **exclusivamente** para elementos que transmiten información (badges de tipo, persona, moneda, estados de saldo). No hay aliases: cada regla CSS referencia directamente la variable que necesita.

> **Excepción — botones de acción semánticos:** los botones que disparan una acción importante pueden usar color: `.btn-peligro` (rojo) para destructivas (ej. Restaurar backup) y `.btn-acento` (indigo) para la acción primaria de una sección (ej. Crear backup). Motivo práctico además del semántico: `.btn-primario` usa `--color-deco-2`, que en dark (`#1e293b`) coincide con `--color-superficie` y queda **invisible** sobre una tarjeta. NO revertir `.btn-acento` a `.btn-primario` "para cumplir la filosofía": reintroduce el bug.

**Dark mode:** `document.documentElement.dataset.theme = 'dark'` activa la paleta oscura. Un script inline en `base.html` (antes del stylesheet) lee `localStorage.getItem('tema')` y setea `data-theme` antes del primer paint, evitando parpadeo. Default: `'light'`.

**Toggle Light/Dark:** botón `#theme-toggle` (clase `.header-icon-btn`) en el cluster `.header-acciones` (derecha del header), visible en todas las páginas. Icono `#theme-icon` = `🌙` en light, `☀` en dark. JS: `inicializarToggleTema()` (en `app.js`) lee `dataset.theme`, pinta el icono inicial, y al click alterna `dataset.theme`, guarda `localStorage.setItem('tema', ...)`, repinta el icono, y llama `window._refrescarColoresSelects()` para actualizar los fondos/textos de los selects de persona y moneda con los colores del nuevo tema. El modo activo es por dispositivo (localStorage); los valores de color son compartidos (config.json).

**UI de edición:** en `settings.html → sección "Paleta de colores"` hay **una sola** `<table>` con columnas Nombre · Uso · Light · Dark. Jinja itera `paleta_meta` (de `app.py`) y agrupa las filas con encabezados por categoría (Marca, Superficies, Estados, Personas, Monedas, Decorativos) vía un dict `grupos` + `namespace` en la plantilla. Cada fila tiene dos `<input type="color">` (`data-tema="light"` y `data-tema="dark"`, `data-key`), tomando valor de `cfg.paleta_light[key]` / `cfg.paleta_dark[key]`. El hex **no** se muestra como texto: se ve solo en el picker. Botón "Guardar paleta" → `POST /api/paleta` (JSON con ambas paletas, valida hex, escribe `config.json`). Al recargar, `base.html` re-inyecta los valores. Cero hex hardcodeado en el HTML.

**Monitor de recursos (relojitos):** panel sticky en `.settings-aside`. 4 gauges SVG (CPU app, RAM app, CPU sistema, RAM sistema) dibujados con dos `<circle>` (`.gauge-bg` + `.gauge-arc` con `stroke-dasharray`/`stroke-dashoffset`). JS `fetchMetrics()` (en `settings.html`) hace `GET /api/metrics` cada 3s y `setGauge()` ajusta arco + valor + color por umbral (verde/amarillo/rojo). Sin historización: solo el valor actual. Pie con PID, estado "En ejecución" y cotización OK/Falló.

Variables actuales (al modificar, actualizar también `config.py DEFAULTS`, `app.py PALETA_META` y la página Settings → Paleta):

| Variable                  | Uso semántico                                  |
|---------------------------|------------------------------------------------|
| `--color-acento`          | Botones primarios, links, focus ring           |
| `--color-acento-oscuro`   | Hover de botones, fondo nav                    |
| `--color-fondo`           | Fondo general                                  |
| `--color-superficie`      | Tarjetas, inputs, modales                      |
| `--color-texto`           | Texto principal                                |
| `--color-texto-muted`     | Texto secundario                               |
| `--color-texto-invertido` | Texto sobre botones/badges de color (acento, éxito, peligro, badges responsable) |
| `--color-borde`           | Bordes, separadores                            |
| `--color-exito`           | Ingresos, OK, semáforo verde                   |
| `--color-alerta`          | Pendiente, advertencia                         |
| `--color-peligro`         | Eliminar, error, saldo negativo                |
| `--color-exito-suave`     | Fondo badge OK                                 |
| `--color-alerta-suave`    | Fondo badge alerta                             |
| `--color-peligro-suave`   | Fondo badge error                              |
| `--color-persona-elias`   | Identificador visual Elías                     |
| `--color-persona-mari`    | Identificador visual Mari                      |
| `--color-persona-leon`    | Identificador visual León (turquesa pastel; rutina, lactancia). Distinto del celeste ARS |
| `--color-moneda-ars`      | Badge AR$, gauge total ARS                     |
| `--color-moneda-usd`      | Badge USD, gauge total USD                     |
| `--color-deco-1..4`       | Grises decorativos: nav, botones, bordes estructurales |

No-color (también en `:root`, NO editables desde Settings): `--fuente-principal`, `--radio-borde`, `--espaciado-base`, `--sombra-card`, `--sombra-focus`, `--sombra-modal`, `--overlay-modal` (fondo de overlays de modal).

**Técnica de tintes con `color-mix` (nueva con el módulo Rutina):** los fondos suaves por tipo de fila se generan con `color-mix(in srgb, var(--color-X) N%, var(--color-superficie))`. Regla: SOLO `var()` adentro del `color-mix` (sigue valiendo "cero hardcode") — así el tinte se adapta solo a light/dark sin variables `-suave` extra. Siempre dejar una línea previa `background: var(--color-superficie)` como fallback para browsers sin soporte. Hoy solo la usa la sección `rut-`.

## Mapa de secciones de `style.css` (línea inicial)

57 banner DEV · 79 variables · 141 reset · 187 layout · 205 topbar · 220 header (incluye `.header-icon-btn` ~269, botón-icono circular reutilizable ☰/tema/campana) · 372 page-header · 389 botones · 480 tabla gastos · 567 badges categoría · 595 sin-datos · 613 forms · 669 resumen · 774 tarjeta saldo · 800 carga rápida · 891 grilla saldos · 946 filtros · 1025 badges tipo · 1049 badges persona · 1065 badges moneda · 1094 dashboard · 1567 edición inline · 1658 envío · 1729 badge envío · 1775 toasts · 1841 banner primer inicio · 1861 banner éxito · 1875 settings · 2008 monitor recursos · 2130 paginación · 2186 form rápido · 2202 responsive mobile · 2338 tabla saldos · 2403 nav mes · 2456 tags fijos (pills `.fijo-tag`, impagos clickeables + pagados grisados + pill Administrar) · 2555 gastos_fijos · 2630 git backup · 2697 fijos en settings · 2764 layout desktop (incluye bloque "Home sin scroll de página": tabla con scroll propio + thead sticky + compactación col izquierda) · 2914 tarjeta saldos · 3040 movimientos card · 3148 select filtro · 3172 gauges (leyenda a la derecha, `.gauges-saldos .gauge-svg` scoped porque el Monitor de Settings redefine `.gauge-svg`) · 3302 paleta settings · 3420 cotización settings · 3489 **settings v2** (layout 2 col sticky · cot-box · paleta tabla única agrupada · backups · fijos chips con switch · relojitos/gauges monitor) · 3910 **calendario** (prefijo `cal-`: paneles, agenda, grilla mensual, alta rápida, switch, modales, tabla Todas, toasts `cal-toast-*` · desktop ≥900px: 2 col `minmax(0,1fr) 416px`, `body.cal-body` alto 100dvh sin scroll de página, scroll interno en agenda/detalle) · 4790 **lactancia** (prefijo `lac-`: aviso, alta, tablero `lac-stats`, listas `lac-item`, checkbox `.lac-check` (tildado por defecto solo si `freezar_reciente`; `.is-off` = bloqueado solo si vencida, `freezable=false` del server → destildada + disabled; partidas viejas pero no vencidas nacen destildadas y se pueden tildar igual) y botón `.lac-btn-freezar` (⬆ combina tildadas), pills `lac-pill-*`, modales, toasts `lac-toast-*` con botón `.lac-toast-accion` Deshacer · estados→paleta: vencida=peligro, vence_pronto=alerta, disponible y en_heladera=exito — **sin variable nueva de paleta** — cierres=deco/muted/acento · desktop ≥900px: `body.lac-body` 100dvh sin scroll de página, grid de 3 columnas `5fr/6fr/5fr` — izq cargar+resumen, centro freezer/heladera con scroll interno, der historial). · ~5445 **rutina** (prefijo `rut-`: topbar sticky propio `.rut-topbar` (top medido por `rutina.js` en cada render = top+alto del `.site-topbar`, z-index 400 bajo el 805 del global), chips `.rut-chip`, días `.rut-dia-btn`, etapas `.rut-etapa-btn`, aviso, tarjetas `.rut-card-ahora` con barra `.rut-barra` — LIENZO DE COLUMNAS: `.rut-canvas-head`/`.rut-col-head` (cabeceras por persona), `.rut-canvas` (alto inline = eje × 1.6 px/min), `.rut-gutter` 40px +`.rut-hora-label`+`.rut-gridline` (reglas de hora), `.rut-cols`/`.rut-col` (una por persona activa, separadas por borde punteado), `.rut-item` absolutos (borde izq 3px `--rut-color`, tintes por kind `--sueno/--noche/--teta/--bano/--guarderia/--custom`, línea `.rut-item-linea` hora+emoji+título 0.95rem+dur, `.rut-item-sub` 0.85rem en bloques ≥68px con line-clamp inline desde JS, `--mini` <38px compacta, `--abierto` dur 0 con mask degradé, `.is-ahora` ring acento; columnas con tope 380px (max-width inline); mobile <768px oculta dur/sub y achica título), `.rut-linea-ahora`, `.rut-popover` (detalle/editor y quitar), `.rut-item-quitar` (✕ modo edición) — franja nocturna `.rut-noche` + filas `.rut-fila` clásicas (espina/veta/editor inline `.rut-editor`, solo las usan la franja y Tareas quitadas), tips `.rut-tips-card`, keyframes `rutinaPulso` · color por persona vía custom property local `--rut-color` en `.rut--leon/.rut--mama/.rut--papa` · tintes por tipo de fila con `color-mix` (ver sección Paleta; incluye `.rut-fila--custom` = tareas añadidas, tinte éxito 8%) · modo edición: `.rut-btn-quitar` (✕), `.rut-quitar-bar` (¿solo hoy o siempre?), `.rut-add-btn`/`.rut-add-form`/`.rut-add-pill`/`.rut-add-input`/`.rut-add-select` (form añadir), `.rut-quitados`/`.rut-quitado-fila`/`.rut-btn-restaurar` (restaurables) · desktop ≥900px: `body.rut-body` (+`:has()`) 100dvh sin scroll de página, grid 2 col `minmax(340px,420px) 1fr` — izq topbar (static) + selectores + "Ahora"; der `.rut-dia-completo` con scroll interno y `.rut-tl-head` sticky · mono = `'Courier New', monospace` hardcodeado como el resto del archivo). Secciones nuevas al final del archivo (reemplazan la vieja `.site-nav`): 6538 **HEADER ACCIONES** (`#menu-toggle` abs-izq · `.header-acciones` abs-der · `.notif-badge` pill roja de la campana, receta de la vieja nav-badge · `@480px` oculta el slogan) · 6591 **SIDEBAR** (`.sidebar` fixed izq `translateX(-100%)`→`.is-abierto`, fondo deco-1, `.sidebar-head`, `.sidebar-nav` con `.nav-activo` scoped = tinte acento color-mix + borde izq 3px) · 6660 **DRAWERS** (`.drawer-backdrop` compartido z-800 · `.drawer-cerrar` ✕ hereda color del head vía `color:inherit` · `body.has-dev-banner` baja los drawers a top:24px) · 6693 **PANEL NOTIFICACIONES** (`.notif-panel` fixed der, fondo superficie · `.notif-item*` icono/cuerpo/modulo/titulo/detalle · severidad `.is-peligro/-alerta/-info` = borde izq + tinte color-mix · `.notif-vacio`) · 6812 **HOME (Inicio)** (prefijo `home-`: `body.home-body`/`:has(.home-page)` = 100dvh sin scroll de página SIEMPRE — sin gate de media query, desktop Y mobile · `.home-page` grid — mobile 2 col (Gastos/Rutina span 2, Lactancia|Calendario), desktop ≥900px `minmax(320px,380px) minmax(280px,1fr) minmax(280px,1fr)` × 2 filas · `.home-card` flex column overflow hidden, `.home-card-head` compacto con link muted "Ver módulo →", `.home-card-body` = única zona con scroll interno · `.home-sheet` = form de movimiento: bottom-sheet fijo en mobile (oculto, `.is-abierta` lo abre, max-height 85dvh) / bloque static siempre visible en desktop · `.home-overlay` z-900 y sheet z-910 (sobre drawers 810, bajo modales 1000) · `.home-btn-nuevo` solo <900px · tarjeta Lactancia: `.home-lac-sec` (título de sección), `.home-lac-item` (ítem compacto, borde izq 3px + tinte `color-mix` por estado — misma correspondencia estado→paleta que el módulo, sin variables nuevas), `.home-lac-vol/-venc/-meta/-usar/-vacio`; `.home-sheet .lac-form-titulo{display:none}` porque el título lo da `.home-sheet-head`). Jerarquía z-index (comentada en topbar): rut-topbar 400 < backdrop 800 < **topbar 805** < drawers 810 < home-overlay 900/home-sheet 910 < modales 1000 < dev-banner/toasts 9999 — el header va arriba del backdrop a propósito (sus botones tienen que quedar clickeables con un drawer abierto; el drawer abierto sigue tapando SU franja del header, ej. la sidebar cubre el ☰, ver `initDrawers()` en `app.js`).

## Funciones JS principales (`static/app.js`)

| Función                         | Propósito                                            |
|---------------------------------|------------------------------------------------------|
| `llenarSelectCategorias()`      | Pobla `<select>` según tipo                          |
| `inicializarCategorias()`       | Bind cambio de tipo                                  |
| `actualizarDescripcionSegunCategoria()` | Auto-llena descripción                       |
| `inicializarFechaHoy()`         | flatpickr + default hoy                              |
| `inicializarFormatoMonto()`     | Separadores miles en input                           |
| `inicializarPersona()`          | Selector visual persona                              |
| `inicializarColoresDinamicos()` | Lee `--color-*` del tema activo; calcula texto claro/oscuro con `textForBg()` (WCAG L>0.35 → oscuro); expone `window._refrescarColoresSelects` |
| `inicializarToggleTema()`       | Botón header Light/Dark; persiste en localStorage; llama `_refrescarColoresSelects()` al cambiar tema |
| `resaltarNavActual()`           | Marca link activo en la SIDEBAR (`.sidebar-nav a`, clase `nav-activo`) |
| `initDrawers()`                 | Abre/cierra sidebar (`#menu-toggle`) y panel notif (`#notif-toggle`); backdrop compartido, exclusión mutua, cierre por ✕/ESC/backdrop/link; al abrir el panel llama `Notif.refrescar()`; sincroniza aria |
| `window.Notif.refrescar()`      | API pública de la campana: `fetch /api/notificaciones` → actualiza `#notif-badge` + re-renderiza `#notif-lista` (createElement, nunca innerHTML). La llaman los módulos tras mutar datos. Ver `CONTEXT_NOTIFICATIONS.md` |
| `initSelectVista()`             | Cambio de vista de tabla                             |
| `initFiltros()` / `initOrden()` | Filtros y ordenamiento de tabla                      |
| `initEdicionInline()`           | Doble click → editar fila in-place                   |
| `activarEdicion()` / `guardarEdicion()` / `cancelarEdicion()` | Ciclo edición |
| `actualizarSaldos()`            | Refresca las 6 celdas de saldos en DOM               |
| `actualizarGauges()`            | Refresca los 3 gauges (arcos/textos/leyendas) sin recargar |
| `inicializarSaldosFecha()`      | Date picker en tarjeta saldos; `fetch /api/saldos` → saldos+gauges a una fecha |
| `crearFilaMovimiento()`         | Renderea fila tras AJAX                              |
| `mostrarToast()`                | Toast confirmación                                   |
| `initFormAjax()`                | Submit sin recarga. Con `data-modo="inline"` (form del Inicio) NO navega: actualiza saldos + toast + reset (re-fija fecha hoy, limpia hidden monto, restaura persona, re-dispara change de tipo) + `cerrarHomeSheet('gastos')`. Sin data-modo: navega a /gastos (comportamiento clásico) |
| `initTagsFijos()`               | Click en tag de fijo impago → precarga form rápido (gasto/Fijo/descripción) y enfoca monto |

`var ordenarTablaFn` expuesta para que `initFormAjax` reordene tras insertar.

## Funciones JS del módulo Calendario (`static/calendario.js`)

Todo en una IIFE: no expone globales ni pisa `fmtFecha`/`mostrarToast` de app.js. Estado local = `window.CAL_DATOS`; cada mutación AJAX devuelve el payload completo fresco y se re-renderiza todo. El estado/próxima fecha NO se recalcula acá (viene del server).

| Función                          | Propósito                                              |
|----------------------------------|--------------------------------------------------------|
| `sumarIntervalo()`               | Espejo de `_act_sumar_intervalo` (clamp fin de mes) — solo para el preview del modal Completar |
| `fmtFecha/fmtMesAnio/fmtIntervalo/textoRelativo` | Formatos es-AR; clave ASCII `anios` → "años" |
| `renderAgenda/renderCalendario/renderDetalle/renderTodas` | Re-render de cada bloque desde `DATOS` |
| `mapaPorDia()`                   | iso → puntos: vencida/proxima/aldia (próxima fecha) + `hecha` (historial) |
| `abrirCompletar/abrirEditor/abrirTodas` + `guardar*` | Modales y mutaciones (`postAccion()` = fetch + `X-Requested-With`) |
| `toast()`                        | Toast propio: `.toast` base + `.cal-toast-ok/info/error` en `#cal-toast-container` |

## Funciones JS del módulo Lactancia (`static/lactancia.js`)

Misma arquitectura que calendario.js: IIFE, estado local desde `window.LAC_DATOS`, `postAccion()` reemplaza DATOS con el payload fresco y llama `renderTodo()`. Estados/vencimientos vienen del server (no se recalculan).

| Función | Propósito |
|---------|-----------|
| `toast(texto, tipo, undoCb)` | Toast propio; con `undoCb` agrega botón "Deshacer" (8 s → POST `/reabrir`) |
| `textoVencFreezer/Heladera()` | Relativos desde `dias_restantes`/`horas_restantes` ("Vence en 5 h"; nunca hora absoluta en heladera) |
| `renderAviso/Tablero/Listas + renderTodo()` | Re-render total desde DATOS; `renderTodo` llama `window.Notif.refrescar()` (campana del header). `DATOS.badge` sigue en el payload por compat, ya no se usa para la nav |
| `cerrarDirecto(id, motivo)` | One-click Usada/Tirar con fecha=hoy + toast Deshacer |
| `freezarSeleccionadas()` | Botón ⬆: junta los checkbox tildados de heladera → POST `/api/lactancia/freezar`; toast con Deshacer que revierte la combinación completa |
| `abrirCerrarFecha/abrirMas/abrirEditor/abrirConfirmEliminar` + `guardar*` | Modales y mutaciones (`postAccion()` = fetch + `X-Requested-With`) |

## Funciones JS del módulo Rutina (`static/rutina.js`)

Misma arquitectura IIFE. Fuentes de verdad: `ETAPAS` (constante: definiciones de rutina por etapa, port del design handoff), `window.RUTINA_ACTIVIDADES` (datos), `AJUSTES`/`TAREAS`/`OCULTOS` = `window.RUT_DATOS` (SQLite; ajustes shape `fecha → etapa → item_id → min`; tareas añadidas y ocultos con convención `fecha '' = permanente`) y localStorage `rutina-ui-v1` (`{sel, dia, etapa}`, por dispositivo). Fechas SIEMPRE locales con `isoLocal()` — nunca `toISOString()` (UTC corre el día después de las 21:00 ART).

| Función | Propósito |
|---------|-----------|
| `calcular()` | Cascada del día: filtra QUITADOS antes de encadenar (`ocultosVista()`; un ítem de León quitado re-encadena los siguientes y sus links de adultos desaparecen con él), luego `run=anchor; start=ajuste??run; end=start+dur` + nocturnas (`inicio noche + off`, dur 25) + `expandir()` de adultos (`link` puro hereda horario Y duración EXACTOS de León — sin piso de 30': un fin inflado pisaría la actividad siguiente del adulto — y NO es editable, ids derivados `mama-*`/`papa-*` jamás se POSTean a `/ajustar`; `link`+`id` propio = default en la ventana de León pero editable, ajuste = clavado; `clock` = minutos absolutos, editable) + `tareasDe()` (tareas añadidas: horario fijo, id `c-<rowid>`, editables, kind `custom`, emoji escapado acá UNA vez) + cadenas de finde + actividad de estimulación del día (seed por fecha). Devuelve también `quitados` (para restaurar) |
| `ocultosVista()` | Set `{item_id: true}` de quitados que aplican al día visible (permanentes + fecha exacta) |
| `ajustar(itemId, min)` | Optimistic: escribe en `AJUSTES` local + `renderTodo()` inmediato + POST `/api/rutina/ajustar` debounced (400 ms por ítem: una ráfaga de −15/+15 = un POST). Si falla, el valor local queda y `renderAviso` muestra "⚠ Sin conexión" |
| `resetDia()` | "↺ Plan original": borra la clave local fecha+etapa + POST `/api/rutina/reset` (NO toca tareas añadidas/quitadas) |
| `ocultarItem/borrarTarea/restaurarItem/crearTarea` | Mutaciones del modo edición: no-optimistas (POST y esperan el payload fresco; acciones poco frecuentes) |
| `postAccion(url, campos)` | fetch con `X-Requested-With`; agrega `desde`/`hasta` (semana del cliente); al ok reemplaza `AJUSTES` con el payload fresco SOLO si no hay cambios locales pendientes; `TAREAS`/`OCULTOS` se reemplazan siempre (no tienen edición local pendiente) |
| `syncAjustes()` | Tick de 30 s (+ al volver visible la pestaña): GET `/api/rutina` → trae ajustes del otro teléfono; se saltea si `document.hidden`, hay POSTs pendientes o `modoEdicion` (no pisar lo tipeado); siempre re-renderiza (reloj vivo) |
| `renderTodo()` | Remide el sticky, captura el form de añadir (`capturarFormAdd`) y re-renderiza las 8 zonas (`renderHeader/Chips/Dias/Etapas/Aviso/Ahora/Timeline/Tips`) + estado del botón ✎. El editor inline y el modo edición son estado (`editando`/`modoEdicion`/`quitando`/`formAdd`) → sobreviven los re-renders; delegación de eventos en contenedores fijos → el scroll no salta |
| `renderTimeline(calc, ...)` | Lienzo de columnas: separa nocturnas, calcula eje (hora en punto ≤ min .. ≥ max, sin tope de alto), arma cabeceras + gridlines + una `.rut-col` por persona activa con ítems absolutos (`top/height` a escala; solape dentro de columna → `left` +12px por nivel) + línea "ahora" + `renderPopover()` + `renderNoche()` |
| `renderPopover(porUser, usuarios, y, altoCanvas)` | Popover superpuesto del lienzo, clampeado: detalle (`sub` completo + rango) con editor −15/+15/Ahora/OK si `editable`, nota si es link; en modo edición muestra el "¿Quitar solo hoy o siempre?" (`quitando`) |
| `renderNoche(nocturnas, enCurso)` | Franja "🌙 Durante la noche": filas `.rut-fila` apiladas con editor inline y ✕ clásicos |
| `renderZonaAdd()` / `renderZonaQuitados()` | Modo edición: botón/form "＋ Añadir tarea" (persona, emoji, título, hora en selects, duración, alcance Solo hoy/Todos los días) arriba del lienzo; lista de quitadas grisadas con ↩ Restaurar al pie |
| `ajustarSticky()` | `.rut-topbar.style.top` = top computado + alto del `.site-topbar` (cubre el corrimiento del banner DEV). Se llama en cada render, en `resize` y en `load`. En desktop el topbar es `position: static` (el `top` inline no afecta) |
| `fmt(min)` / `fmtDur(dur)` | `HH:MM` módulo 1440 (nocturnas cruzan medianoche) / "45′", "1 h 30′" |

Reglas de vista: tarjetas "Ahora" y resaltado en vivo SOLO si `dia === hoy && etapa === 'actual'`; si no, aviso 👀 contextual. La fila "Sueño nocturno" queda activa desde su inicio hasta las 05:00. Sin toasts: los ajustes son inmediatos y reversibles. Al quitar (✕): tarea añadida "solo hoy" se borra directo; el resto pregunta "¿Solo hoy o Siempre?" ("Siempre" en una tarea añadida permanente la borra de raíz; en ítems del plan crea un oculto permanente).

## Reglas específicas frontend
1. **Cero hardcode de color**. Solo `var(--color-...)`. Cero aliases: cada regla CSS nombra la variable real que necesita. Excepciones: (a) valores en `style.css :root` son fallbacks legítimos; (b) `login.html` usa hardcode (standalone pre-auth, sin config); (c) `.dash-toggle-btn.activo { color: #ffffff }` intencional (4.47:1 en dark). **Filosofía**: solo deco-1..4 para chrome estructural; colores llamativos únicamente para información.
2. Templates extienden `base.html` con bloques `{% block titulo %}` y `{% block contenido %}`.
3. AJAX usa header `X-Requested-With: XMLHttpRequest`. Si está, backend responde JSON.
4. Cache busting: `?v={{ static_version }}` en `<link>` y `<script>` (versión = mtime del archivo).
5. flatpickr y CDN externos: solo lo ya importado en `base.html`. No agregar libs sin acordar.
6. Mobile breakpoint: `< 768px` (sección 2202 de `style.css`). Desktop dos columnas: `>= 900px` (2764).

## Al modificar este dominio, actualizar:
- Tabla de paleta si se agrega/renombra variable.
- Mapa de secciones si se agrega un bloque nuevo grande en CSS.
- Tabla de funciones JS si se agrega función pública.
- Lista de templates si se agrega/elimina archivo.
